import { BackgroundButtonDirective } from './background-button.directive';

describe('BackgroundButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundButtonDirective();
    expect(directive).toBeTruthy();
  });
});
